:set -XBlockArguments
:set -XLambdaCase
:set -XOverloadedStrings
:set -XPackageImports
:set -XTupleSections

import Control.Applicative (some)

import Text.Megaparsec (runParserT, eof, parseTest)
import Text.Megaparsec.Char (string)
import Text.Megaparsec.Parsers (unParsecT, sepBy1)
import Text.Parser.Token

import Data.Parsing

import Dom.RequestReply

import Ground.Expr
import Ground.Table
