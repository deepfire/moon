var process = {
    env: {
        NODE_ENV: "dev"
    }
};

function arg_mount() { return "luna-node-editor"; }

__dirname = ".";

var atom = {
    config: {
        // get = function (x) {
        //     "luna-studio.typecolors_h"
        // }
    }
};
